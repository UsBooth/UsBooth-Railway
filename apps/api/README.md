# UsBooth API

Reserved for the production API service.

Phase 1 API responsibilities will include authentication, user accounts, permanent Booth identities, Booth sessions, participants, memories, and realtime/WebRTC signaling.

The API will be deployed as a separate Render service when the backend slice is ready.
